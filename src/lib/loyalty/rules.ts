// The bounds of a visit-card program, shared by the route that saves one and
// the form that edits it — and matching `loyalty_programs`' check constraints,
// which are what finally refuse anything else.
export const GOAL_MIN = 2;
export const GOAL_MAX = 50;
export const REWARD_MAX = 80;
