# ═══════════════════════════════════════════════════════════════════════════
# PRODUCTION environment — copy to `.env.production.local` and fill in.
# Point this at your PRODUCTION Supabase project (a DIFFERENT project from dev).
# Used by `pnpm build` / `pnpm start` and the `db:*:prod` scripts.
# This file (.example) is committed; .env.production.local is gitignored.
# (On Vercel you set these as Production environment variables instead.)
# ═══════════════════════════════════════════════════════════════════════════

# ── Supabase (prod project) ─────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROD-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxx
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxx

# ── Stripe (LIVE keys for production) ───────────────────────────────────────
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxxxxx
STRIPE_SECRET_KEY=sk_live_xxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxx

# ── Database (direct Postgres, for the db:*:prod scripts) ───────────────────
DATABASE_URL=postgresql://postgres.YOUR-PROD-REF:YOUR-DB-PASSWORD@aws-0-YOUR-REGION.pooler.supabase.com:5432/postgres
